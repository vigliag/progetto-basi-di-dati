<%= simple_form_for([:admin, @admin_screen]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :avaiable %>
    <%= f.input :seats_string %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
